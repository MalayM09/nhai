# NHAI Biometric — Claude Code Instructions

## Track ownership (CRITICAL)

This repo has two owners on the same branch:

| Owner | Files |
|---|---|
| **Sahil (you)** | `mobile_app/` |
| **Malay (ML)** | `ml_pipeline/`, `ml_pipeline/notebooks/`, `ml_pipeline/evaluation/` |
| **Both sign off** | `shared_contracts/` |
| **Append-only** | `communication/commit_log.md` |

**NEVER edit, delete, or create files under `ml_pipeline/` or `ml_pipeline/evaluation/`.** Do not touch Malay's notebooks, reports, training artifacts, or requirements. If a task seems to require changing those files, stop and ask Sahil first.

`shared_contracts/` changes require agreement from both tracks before touching.
